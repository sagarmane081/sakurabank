# Frontend (Phase 4)

React 18 + TypeScript + Vite. Scaffolded in Phase 4 (`npm create vite@latest . -- --template react-ts`).
Kept empty until then per scope discipline: frontend <= 20% of total effort.
